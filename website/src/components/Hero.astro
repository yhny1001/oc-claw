<section class="hero">
  <img src="/app-icon.png" alt="OC-Claw app icon" class="app-icon" width="180" height="180" />
  <div class="hero-copy">
    <h1>OC-Claw</h1>
    <p class="tagline">A macOS notch desktop pet that monitors your AI coding agents — OpenClaw and Claude Code — in real time.</p>
  </div>
  <div class="btn-wrap">
    <a href="https://github.com/rainnoon/oc-claw" class="btn">
      <svg class="github-icon" xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
        <path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/>
      </svg>
      View on GitHub
    </a>
  </div>
  <p class="badge"><span class="badge-label">OPEN SOURCE</span><span>Tauri v2 · React · Rust</span></p>
</section>

<section class="features">
  <div class="feature-card">
    <div class="feature-emoji">🐾</div>
    <div class="feature-title">Notch Pet</div>
    <div class="feature-desc">A character lives beside your MacBook notch, animating when agents work and sleeping when idle.</div>
  </div>
  <div class="feature-card">
    <div class="feature-emoji">🤖</div>
    <div class="feature-title">Agent Monitoring</div>
    <div class="feature-desc">Auto-discovers OpenClaw agents and Claude Code sessions. View session lists, chat history, and token stats.</div>
  </div>
  <div class="feature-card">
    <div class="feature-emoji">🎨</div>
    <div class="feature-title">Character System</div>
    <div class="feature-desc">Create custom GIF characters and pair them with different agents. Built-in GIF maker included.</div>
  </div>
</section>

<script>
  const SHAPES = ['star4', 'star6', 'cross', 'circle', 'dot', 'diamond', 'ring'];
  const COLORS = [
    '#f47b38', '#ff9a5d', '#ffb180',
    '#a78bfa', '#818cf8',
    '#34d399', '#6ee7b7',
    '#f472b6', '#fb7185',
    '#60a5fa', '#38bdf8',
    '#fbbf24', '#facc15',
  ];

  const SHAPE_PATHS = {
    star4: 'M8 0L9.5 6.5 16 8 9.5 9.5 8 16 6.5 9.5 0 8 6.5 6.5Z',
    star6: 'M8 0L9.2 5.5 14 2 10.5 6.8 16 8 10.5 9.2 14 14 9.2 10.5 8 16 6.8 10.5 2 14 5.5 9.2 0 8 5.5 6.8 2 2 6.8 5.5Z',
    cross: 'M7 0h2v16H7zM0 7h16v2H0z',
    diamond: 'M8 2L14 8 8 14 2 8Z',
  };
  const CIRCLE_SHAPES = {
    circle: { r: 3.5, sw: 1.5, fill: false },
    dot: { r: 2.5, sw: 0, fill: true },
    ring: { r: 5, sw: 1, fill: false },
  };

  const MAX_PARTICLES = 40;
  let liveCount = 0;

  function createSparkleEl(shape, color, size) {
    const svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
    svg.setAttribute('width', String(size));
    svg.setAttribute('height', String(size));
    svg.setAttribute('viewBox', '0 0 16 16');

    if (SHAPE_PATHS[shape]) {
      const path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
      path.setAttribute('d', SHAPE_PATHS[shape]);
      if (shape === 'diamond') {
        path.setAttribute('stroke', color);
        path.setAttribute('stroke-width', '1.5');
        path.setAttribute('fill', 'none');
      } else {
        path.setAttribute('fill', color);
      }
      svg.appendChild(path);
    } else {
      const c = CIRCLE_SHAPES[shape];
      const circle = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
      circle.setAttribute('cx', '8');
      circle.setAttribute('cy', '8');
      circle.setAttribute('r', String(c.r));
      if (c.fill) {
        circle.setAttribute('fill', color);
      } else {
        circle.setAttribute('stroke', color);
        circle.setAttribute('stroke-width', String(c.sw));
        circle.setAttribute('fill', 'none');
      }
      svg.appendChild(circle);
    }

    const el = document.createElement('div');
    el.className = 'sparkle';
    el.appendChild(svg);
    return el;
  }

  function spawnSparkle(container, rect) {
    if (liveCount >= MAX_PARTICLES) return;

    const shape = SHAPES[Math.floor(Math.random() * SHAPES.length)];
    const color = COLORS[Math.floor(Math.random() * COLORS.length)];
    const size = 6 + Math.random() * 12;

    const padX = 60;
    const padY = 50;
    const x = -padX + Math.random() * (rect.width + padX * 2);
    const y = -padY + Math.random() * (rect.height + padY * 2);

    const inBtn = x > 0 && x < rect.width && y > 0 && y < rect.height;
    if (inBtn && Math.random() > 0.3) return;

    const el = createSparkleEl(shape, color, size);
    el.style.left = `${x}px`;
    el.style.top = `${y}px`;

    const drift = -20 + Math.random() * 40;
    const rise = -15 + Math.random() * -25;
    const dur = 600 + Math.random() * 900;
    const rot = Math.random() * 180 - 90;

    el.style.setProperty('--drift', `${drift}px`);
    el.style.setProperty('--rise', `${rise}px`);
    el.style.setProperty('--rot', `${rot}deg`);
    el.style.animationDuration = `${dur}ms`;

    liveCount++;
    container.appendChild(el);
  }

  const wrap = document.querySelector('.btn-wrap');
  if (wrap) {
    let interval = null;

    wrap.addEventListener('animationend', (e) => {
      if (e.target.classList.contains('sparkle')) {
        e.target.remove();
        liveCount--;
      }
    });

    wrap.addEventListener('mouseenter', () => {
      const rect = wrap.getBoundingClientRect();
      clearInterval(interval);
      for (let i = 0; i < 8; i++) setTimeout(() => spawnSparkle(wrap, rect), i * 50);
      interval = setInterval(() => spawnSparkle(wrap, rect), 80);
    });

    wrap.addEventListener('mouseleave', () => {
      clearInterval(interval);
      interval = null;
    });
  }
</script>
